{
  "name": "torna",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Instant card refunds on Monad — collateral-backed liquidity pool",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify:bundle": "tsx scripts/verify-bundle.ts",
    "dev": "pnpm --filter @torna/web dev",
    "build": "pnpm --filter @torna/web build"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
