# Torna — 개발 PRD

**Monad Metropolis · Consumer Products & Payments**
작성 2026-09-19 · 작성자 이A(서진) · 문서 언어 한글

---

## 0부 · 시작하기 전에

### 1. 이 문서를 읽는 법

이 문서는 **무엇을 만드는지**를 정합니다. **왜 그렇게 설계했는지**는 Notion에 있습니다. 설계 근거가 궁금하면 각 절에 달린 Notion 참조를 따라가세요. 이 문서에 옮겨 적지 않은 이유는, 수치가 바뀔 때 고칠 곳을 한 군데로 유지하기 위해서입니다.

| 역할 | 읽을 곳 |
|---|---|
| **A(서진)** (프론트엔드) | 전체 |
| **B(민서)** (스마트 컨트랙트) | 1~10장, 12장, 14~18장 |
| **C(민재)** (어댑터 · 원장 DB) | 1~10장, 13장, 14~18장 |

**10장(공유 계약과 착수 합의)은 세 사람 모두 반드시 읽습니다.** 여기가 서로의 인터페이스이고, 개발이 꼬이는 지점의 대부분이 여기서 시작됩니다.

처음 읽으실 때는 1~10장을 훑고, 자기 역할 장의 「첫 30분」부터 손을 대시면 됩니다.

---

### 2. GitHub 운영 규칙

#### 브랜치

```
main        제출 가능한 상태만. 태그를 붙인다.
develop     통합 지점. 여기서는 항상 데모가 돌아야 한다.
feat/*      개인 작업 브랜치
```

> `develop`은 A(서진)이 먼저 만들어 올려둡니다. 팀원은 클론 후 바로 `git checkout develop` 하면 됩니다.

브랜치 이름은 `feat/<영역>-<내용>` 형식을 씁니다. 영역은 `contract` · `adapter` · `ledger` · `front` · `shared` 다섯 가지입니다.

```
feat/contract-advance
feat/adapter-signing
feat/front-timeline
```

브랜치는 폴더가 아니라 **그 시점 코드의 사본**입니다. 「develop에서 브랜치를 판다」는 말은 **develop의 현재 상태를 그대로 복사해 내 작업 공간을 만든다**는 뜻입니다.

```
main      ──●───────────────────────●──   제출 가능한 상태만
             \                     /
develop   ────●────●────●────●────●───    셋이 합치는 곳
               \       /  \      /
feat/*          ●──●──●    ●──●──         각자 작업
```

셋이 `develop`에 직접 커밋하면 서로 덮어씁니다. 그래서 각자 자기 브랜치에서 작업하고, 다 되면 `develop`으로 합칩니다.

#### 작업 흐름

**① 처음 한 번** — 클론하고 develop으로 갑니다.

```bash
git clone https://github.com/HYBLOCK-LAB/torna.git
cd torna
git checkout develop
```

**② 작업을 시작할 때마다** — develop 최신 상태를 받고, **거기서 새 브랜치를 만듭니다.**

```bash
git checkout develop
git pull origin develop
git checkout -b feat/contract-advance   # ← develop을 복사한 새 브랜치가 생기고 그리로 이동
```

`-b`가 「새로 만들면서 이동」입니다. 이 시점에 `feat/contract-advance`의 내용은 `develop`과 완전히 같습니다. 여기서부터 내 변경이 쌓입니다.

**③ 작업하고 커밋합니다.**

```bash
git add .
git commit -m "[contract] add AdvanceIssued event"
```

**④ 하루 한 번** — 그동안 남이 develop에 합친 것을 내 브랜치로 가져옵니다.

```bash
git checkout feat/contract-advance
git pull origin develop      # merge로 가져옵니다. rebase는 쓰지 마세요
```

충돌이 나면 **여기서** 풉니다. 이 단계를 건너뛰면 PR을 올릴 때 한꺼번에 터집니다.

**⑤ 올리고 PR을 엽니다.**

```bash
git push -u origin feat/contract-advance
# GitHub에서 feat/contract-advance → develop 으로 PR
```

**⑥ 머지 후** — develop에서 데모가 도는지 확인합니다.

**④를 하루 한 번 이상 하세요.** 충돌은 미룰수록 커집니다. 일주일 치를 한 번에 합치면 그날 하루를 통째로 씁니다.

한 브랜치에서 여러 날 작업해도 됩니다. 다만 **하나의 브랜치는 하나의 주제**로 두세요. 컨트랙트 작업과 어댑터 작업을 한 브랜치에 섞으면 리뷰도 되돌리기도 어려워집니다.

#### main에 올리는 시점

| 태그 | 시점 |
|---|---|
| `v0.1-integration` | 1차 통합이 끝났을 때 |
| `v0.5-scenarios` | 시나리오 전체 실행 성공 후 |
| `v1.0-submission` | 제출 직전 |

태그를 붙이는 이유는 **되돌아갈 지점을 만들기 위해서**입니다. 제출 전날 뭔가 깨졌을 때 마지막 정상 태그로 돌아갈 수 있어야 합니다.

#### 규칙

| 규칙 | 이유 |
|---|---|
| `develop` · `main`에 직접 push 금지 | 되돌리기가 어려워집니다 |
| 내 영역 밖 파일은 고치지 않는다 | 고쳐야 하면 소유자에게 말합니다. 소유권은 9장 |
| `shared/` 변경 PR은 소유자 승인 필수 | 세 사람의 인터페이스입니다 |
| `.env`는 절대 커밋하지 않는다 | `.gitignore`에 이미 들어 있습니다. 확인만 하세요 |
| 스냅샷 번들은 손으로 고치지 않는다 | 스크립트 출력만 커밋합니다 |
| 공유 브랜치에 force push 금지 | 남의 커밋이 사라집니다 |

#### 커밋 메시지는 영문으로

**레포 자체가 제출물이라 커밋 이력이 노출됩니다.** 심사위원이 열어볼 수 있는 자리입니다.

```
[contract] add AdvanceIssued event
[adapter]  fix decimal conversion 2 -> 6
[front]    render timeline notes per screen
```

영역 태그 + 한 줄이면 충분합니다. 완벽한 영어일 필요는 없습니다.

#### PR

무겁게 가지 않습니다.

- 제목 한 줄, 본문에 바꾼 내용 3줄 이내
- `shared/`를 건드렸으면 소유자를 리뷰어로, 아니면 A(서진)
- 승인 없이 머지해도 되지만 **팀에 알립니다**

세 명이라 리뷰 대기로 막히는 게 더 손해입니다.

#### 충돌이 났을 때

- **`shared/` 충돌** — 혼자 풀지 마세요. 소유자와 같이 봅니다. 여기서 잘못 합치면 세 사람이 동시에 깨집니다.
- **내 영역 충돌** — 내가 풉니다.
- **도저히 안 풀릴 때** — 브랜치를 새로 파고 필요한 파일만 복사해 오는 게 제일 안전합니다. 억지로 되돌리다 더 꼬이는 경우가 많습니다.

커밋 전에 꼬였다면 `git stash`로 잠시 치워두고 상태를 정리하세요.

---

### 3. 언어 정책

**제품의 기본 언어는 영어입니다.** 글로벌 해커톤이고 심사위원이 영어로 읽습니다.

| 산출물 | 언어 |
|---|---|
| 화면에 보이는 모든 문구 | **영문** |
| `README.md` | **영문** |
| 커밋 메시지 · PR | **영문** |
| 컨트랙트 NatSpec 주석 | **영문** |
| 이벤트 · 함수 · 에러 이름 | **영문** |
| 3분 기술 데모 · 2분 피치 스크립트 | **영문** |
| 아키텍처 다이어그램 라벨 | **영문** |
| 이 PRD · Notion 문서 | 한글 |
| 팀 내부 대화 | 한글 |

기준은 단순합니다. **심사위원이 볼 수 있는 것은 영문, 팀이 보는 것은 한글.**

#### 화면 문구는 코드에 직접 박지 않습니다

모든 화면 문구는 `shared/copy.ts`에 키로 분리하고 `en`과 `ko`를 함께 둡니다.

```ts
export const COPY = {
  'scen.3.title': {
    en: 'Correlated loss → cap triggered',
    ko: '상관 손실 → 상한 발동',
  },
} as const;
```

라이브러리는 쓰지 않습니다. 객체 하나와 `lang` 상태면 됩니다.

**레이아웃은 영문 기준으로 잡습니다.** 같은 내용을 영어로 쓰면 한국어보다 30~50% 길어집니다. 한글 기준으로 폭을 맞춰놓고 영문을 넣으면 전부 깨집니다.

언어 토글(EN / KO)은 **스트레치 항목**입니다. 제출 전에 여유가 있을 때만 붙입니다. 문구를 키로 분리해 두었으므로 나중에 붙이는 데 30분이면 됩니다.

#### 번역하지 말고 영어로 쓸 것

설계 논거 문장들은 기계번역하면 힘이 빠집니다.

> 상한은 손실을 없애는 장치가 아니라, 한 사건이 풀 전체를 무너뜨리지 못하게 막는 장치입니다.

이런 문장이 이 프로젝트의 설득력입니다. 영어로 처음부터 쓰고, 한글 원문은 Notion에 근거로 남겨둡니다.

---

### 4. 용어집

한글 정의와 영문 대응어입니다. **영문 대응어는 코드 · README · 영상에서 그대로 씁니다.** 세 사람이 같은 것을 다르게 부르기 시작하면 그때부터 꼬입니다.

| 한글 | 영문 | 뜻 |
|---|---|---|
| 발급사 | issuer | 카드를 발급하는 회사. 사용자의 잔액을 관리한다 |
| 매입사 | acquirer | 가맹점 쪽 정산을 담당하는 회사. 환불금이 흘러오는 상류 |
| 유동성 공급자 | liquidity provider (LP) | 풀에 자금을 예치하고 수수료를 받는 주체 |
| 검증자 | verifier | 손실 여부를 판정하는 권한. 발급사와 분리되어 있다 |
| 선지급 | advance | 풀이 발급사에 환불금을 먼저 주는 것 |
| 상환 | repayment | 실제 정산금이 도착해 발급사가 풀에 갚는 것 |
| 담보 | collateral | 발급사가 미리 예치해 둔 자금. 손실 1순위 부담 |
| 마진 | margin | 손실이 났을 때 발급사가 부담하는 몫 (선지급액의 20%) |
| 워터폴 | waterfall | 손실을 흡수하는 순서. 마진 → 준비금 → LP → 상한 |
| 준비금 | reserve | 프로토콜이 쌓아둔 손실 흡수 전용 자금 |
| 상한 유예 | cap held | 단일 사건 상한을 넘어 판정을 미뤄둔 금액 |
| 회수 | recovery | 부실화된 건에서 나중에 돌려받는 금액 |
| 시점 | timepoint | 데모에서 한 번에 보여주는 누적 상태의 한 단계 |
| 스냅샷 | snapshot | 한 시점의 전체 상태를 담은 JSON 파일 |
| 환불 키 | refundKey | 환불 건의 온체인 식별자. `keccak256`으로 만든다 |
| 어댑터 | adapter | 카드사 데이터를 온체인 형식으로 바꾸는 모듈 |

**「시점」이라는 표현은 운영 화면과 시나리오에만 씁니다.** 사용자 앱 영역에서는 쓰지 않고 「환불 체험」 · 「다시 보기」 · 「돌아가기」로 적습니다.

---

## 1부 · 공통 기반

### 5. 무엇을 만드는가

카드 환불이 확정되어도 실제 정산금이 도착하기까지 영업일 3~5일이 걸립니다. 그동안 사용자의 구매력은 묶여 있습니다. **Torna는 담보로 뒷받침되는 공동 유동성 풀이 발급사에 환불금을 선지급해, 사용자의 잔액을 며칠이 아니라 몇 초 만에 복원합니다.** 사용자는 아무것도 누르지 않고, 지갑도 USDC도 가스비도 보지 않습니다.

> 설계 근거 → Notion 1번 「문제 정의와 시장 검증」, 2번 「왜 블록체인인가」

#### 만드는 것

**화면 다섯 개짜리 웹 데모 하나**입니다.

| 화면 | 보여주는 것 |
|---|---|
| 사용자 앱 | 카드 앱. 취소하면 잔액이 즉시 복원된다 |
| 발급사 콘솔 | 담보 · 유효 한도 · 환불 포지션 |
| LP 대시보드 | 자금 구성 · 손실 흡수 · 출금 |
| 검증자 | 손실 판정 |
| 공개 뷰 | 풀 현황 · 위험 · 운영 규칙 |

다섯 화면이 **하나의 온체인 원장**을 봅니다. 이것이 이 데모의 핵심 주장입니다.

#### 최종 제출물

```
1. 배포된 데모 웹사이트 (Vercel)
2. GitHub 레포
3. Monad Testnet에 배포된 컨트랙트 + 약 800건의 트랜잭션 기록
4. 3분 기술 데모 영상 (영문)
5. 2분 피치 (영문)
```

---

### 6. 환불 한 건이 겪는 일

**이 절 하나만 이해하면 자기 코드가 어디에 들어가는지 알 수 있습니다.**

```
① 취소          사용자가 호텔 예약을 취소한다                     [A(서진)]
② 환불 확정      카드사 원장에 환불 1,000이 확정으로 기록된다        [C(민재)]
③ 변환 · 서명    어댑터가 온체인 형식으로 바꾸고 EIP-712 서명       [C(민재)]
④ 선지급        컨트랙트가 풀에서 발급사로 1,000을 보낸다          [B(민서)]
⑤ 잔액 복원      카드사 원장이 사용자 잔액을 300 → 1,300으로       [C(민재)]
⑥ 앱 표시       사용자 화면에 복원된 잔액이 뜬다                   [A(서진)]
⑦ T+5 상환      정산금이 도착해 발급사가 풀에 1,000을 갚는다        [B(민서)]
```

⑦이 정상 경로입니다. 여기서 벗어나는 경우가 시나리오 2~9입니다 — 돈이 안 돌아오거나(손실), 여러 건이 한꺼번에 안 돌아오거나(상관 손실), 늦게 돌아오거나(지연), 기록이 어긋나거나(DB 실패), 애초에 잘못된 요청이거나(거절).

②와 ⑤가 같은 DB라는 점에 주의하세요. **C(민재)는 나가는 길과 돌아오는 길을 둘 다 만듭니다.**

> 상세 → Notion 3번 「사용자 경험과 원장 구조」

---

### 7. 확정 수치와 파라미터

**아래 값은 확정입니다. 임의로 바꾸지 마세요.** 바꿔야 할 이유를 발견했다면 먼저 팀에 알리고, 바꾸면 스냅샷 번들과 문서를 전부 다시 맞춰야 합니다.

#### 프로토콜 파라미터

| 파라미터 | 값 | 의미 |
|---|---|---|
| 발급사 담보율 | 15% | 미상환 총액 대비 |
| 발급사 손실 부담률 | 20% | 손실 시 1순위 부담 |
| 건당 수수료 | 0.3% | 자금 사용료 + 위험 인수료 + 프로토콜 마진 |
| 수수료 배분 | LP 80% / 준비금 13.3% / 프로토콜 6.7% | |
| 단일 사건 손실 상한 | LP 예치금의 20% | 한 사건이 풀 전체를 무너뜨리지 못하게 |
| 유휴자금 운용 상한 | 50% | 운용분이 전부 묶여도 정상 운영 가능해야 |
| 발급사 집중 한도 | 40% | 단, 발급사가 적을 땐 `1 ÷ 발급사 수`가 우선 |
| 풀 매입사 노출 한도 | 풀 가능액 × 50% | **신규 선지급에만 적용.** 기존 물량 소급 없음 |
| LP 예치 한도 | 평균 미상환 ÷ 30% | 목표 가동률이 정하는 총 예치 상한 |
| 단일 LP 집중 한도 | 25% | 한 LP의 이탈이 풀을 흔들지 못하게 |
| 신규 발급사 램프업 | 첫 30일 한도의 50% | 가장 위험한 순간은 첫 거래 |
| 준비금 출범 시드 | 500 | |

**유효 한도 계산식**

```
유효 한도 = min( 담보 ÷ 15% × 램프업계수,
                 풀 가능액 × max(40%, 1 ÷ 발급사 수) )

풀 가능액 = LP 평가액 − 외부 운용 잔액
LP 평가액 = 예치금 + 누적 수수료 − 손실
```

> 담보율 15%가 어떻게 나왔는지, 왜 상한이 20%인지 → Notion 6번 「유동성·위험·수익 구조」

#### 시점별 검증 수치

**단위 USDC, 반올림.** 인트로가 안내하는 정규 경로 기준입니다. 사용자 앱 체험에서 `REF-2026-001`이 실제 포지션으로 생성되므로 이후 모든 누적 수치가 이를 포함합니다.

| 시점 | LP 평가액 | 미상환 | 준비금 | LP 누적손실 | 누적건수 | 누적 선지급 | 손실률 | 정상상환률 |
|---|---|---|---|---|---|---|---|---|
| 0 사용자 체험 | 10,002 | 0 | 500 | 0 | 1 | 1,000 | 0.00% | 100.0% |
| 1 | 10,878 | 1,000 | 646 | 0 | 366 | 366,000 | 0.00% | 100.0% |
| 2 | 10,725 | 0 | 0 | 154 | 366 | 366,000 | 0.27% | 99.7% |
| 3 | 9,136 | 2,000 | 0 | 1,752 | 370 | 370,000 | 0.81% | 99.2% |
| 3b | 9,536 | 0 | 0 | 1,352 | 370 | 370,000 | 0.76% | 98.6% |
| 4 | 9,546 | 4,000 | 2 | 1,352 | 374 | 374,000 | 0.75% | 98.6% |
| 4b | 7,636 | 1,000 | 2 | 1,082 | 374 | 374,000 | 0.68% | 98.7% |
| 5 | 7,639 | 1,000 | 2 | 1,082 | 375 | 375,000 | 0.67% | 98.7% |
| 6 | 7,639 | 1,000 | 2 | 1,082 | 375 | 375,000 | 0.67% | 98.7% |
| 7 | 7,639 | 1,000 | 2 | 1,082 | 375 | 375,000 | 0.67% | 98.7% |
| 8 | 7,639 | 1,000 | 2 | 1,082 | 375 | 375,000 | 0.67% | 98.7% |
| 9 | 7,644 | 3,000 | 3 | 1,082 | 377 | 377,000 | 0.67% | 98.7% |
| 9b | 16,311 | 3,000 | 3 | 1,082 | 377 | 377,000 | 0.67% | 98.7% |

**보조 사실**

- 상한 유예는 시점 3에서만 1,600이고 나머지는 0
- 외부 운용 잔액은 시점 6부터 3,637
- 시점 4에서 LP-03(지분 20%)이 전액 출금을 요청해 즉시 지급분만 나가고, 시점 4b의 D+5 정산에서 대기분이 지급되며 예치금이 10,000 → 8,000이 된다
- 시점 4b에서 만기 도래 3건 3,000이 상환된다 — 출금 재원은 여기서 나오며 신규 선지급을 침범하지 않는다
- 시점 9b에서 LP-04 4,166 / LP-05 4,166 / LP-06 335 수용, 합계 13,333 거절
- 시점 9b 이후 유효 한도: HYBRID 5,069 / NOVA 3,000 / MERIDIAN 2,500 / KITE 2,000 / AURA 0(Suspended)
- 시점 3b에서 원금 4,000 − 회수 2,200 = 최종 손실 1,800 (담보 600 + 풀 1,200)
- 전 구간 요청 거절은 시점 8의 3건뿐

**이 표가 인수 조건입니다.** 실제 실행 결과가 이 값과 일치하지 않으면 컨트랙트나 어댑터에 버그가 있다는 뜻입니다. 검증 스크립트가 이걸 자동으로 확인합니다.

#### 참여 기관

| 발급사 | 지역 | 매입사 | 담보 | 비고 |
|---|---|---|---|---|
| HYBRID Travel Card | 북미·유럽 | α | 3,000 | 데모 주인공 |
| AURA Travel Card | 아시아 | β | 600 | 시점 3에서 사고 |
| NOVA Travel Card | 중동·아프리카 | γ | 900 | 시점 9에서 등록 |
| MERIDIAN Travel Card | 남미 | δ | 750 | 시점 9에서 등록 |
| KITE Travel Card | 오세아니아 | ε | 600 | 시점 9에서 등록 |

LP는 LP-01 5,000 / LP-02 3,000 / LP-03 2,000으로 시작합니다.

모두 가상의 회사입니다.

#### 손실 처리 규칙

**근거는 Notion 6번에 있고, 여기 있는 것은 그대로 구현할 규칙입니다.**

**① 워터폴 — 손실 1건이 확정될 때**

```
발급사 마진   margin   = amount × 20%      → 그 발급사 담보에서 차감
풀 노출액     coverage = amount − margin

coverage 를 위에서부터 흡수한다
  1. 프로토콜 준비금   min(reserve, coverage)
  2. LP 시니어        남은 전부
```

담보율은 15%인데 부담률은 20%입니다. **이 간격 때문에 담보가 마진을 다 못 내는 경우가 생기고, 부족분은 풀이 떠안습니다.** 시점 3b의 200이 그 경우입니다.

**② 단일 사건 상한**

```
상한 = LP 예치금 × 20%

같은 매입사의 부실 포지션을 순서대로 처리한다
  누적확정 + coverage ≤ 상한  →  확정   CoveredLoss, 누적확정 += coverage
  아니면                      →  보류   CapHeld,   capHeld  += coverage
```

보류는 손실이 아니라 **판정을 미뤄둔 상태**입니다. 회수 결과에 따라 나중에 정산됩니다.

**③ 회수 정산**

```
회수율 r, 대상 = 같은 매입사의 CoveredLoss + CapHeld 포지션

미회수        un_i          = amount_i × (1 − r)
마진 필요액    marginNeed    = Σ min(margin_i, un_i)
실제 마진     marginApplied = min(marginNeed, 발급사 담보)
부족분        shortfall     = marginNeed − marginApplied     ← 풀이 떠안는다
풀 필요액     poolNeed      = Σ max(0, un_i − margin_i)
최종 풀부담    finalPool     = poolNeed + shortfall

상한 유예 해제  capHeld → 0
기인식 손실     recognized = Σ (CoveredLoss 였던 건의 coverage)
차액           delta = recognized − finalPool

delta > 0  →  부담의 역순으로 환입한다: LP 시니어 먼저, 남으면 준비금
delta < 0  →  차액만큼 추가 흡수한다: 준비금 먼저, 남으면 LP

대상 포지션 전부 → RecoveryRecorded
```

시점 3b의 검증: 원금 4,000 − 회수 2,200 = 최종 손실 1,800 = 담보 600 + 풀 1,200.

#### 한도 검사 규칙

```
풀 가능액  = LP 평가액 − 외부 운용 잔액
LP 평가액  = 예치금 + 누적 수수료 − 손실
```

**선지급할 때** — 셋을 순서대로 검사하고, 하나라도 걸리면 `AdvanceRejected` 를 내보냅니다.

| 검사 | 조건 | 거절 코드 |
|---|---|---|
| 발급사 상태 | `Suspended` 가 아닐 것 | 8 |
| 발급사 유효 한도 | 그 발급사 미상환 + amount ≤ **min(담보 ÷ 15% × 램프업계수, 풀 가능액 × max(40%, 1 ÷ 발급사 수))** | 6 |
| 매입사 노출 한도 | 그 매입사 미상환 + amount ≤ **풀 가능액 × 50%** | 7 |

**매입사 노출 한도는 신규 선지급에만 적용합니다.** 이미 나가 있는 물량에 소급해 회수하지 않습니다.

「발급사 집중 한도 40%」는 별도의 검사가 아니라 **유효 한도 식 안에 들어 있는 값**입니다. 기관이 적을 때는 `1 ÷ 발급사 수`가 더 커서 그쪽이 상한이 됩니다.

**예치할 때** — 둘을 검사하고, 걸리면 `DepositRejected` 를 내보냅니다.

| 검사 | 조건 | 거절 코드 |
|---|---|---|
| 풀 예치 한도 | 총예치금 + amount ≤ **max(미상환, 5,000) ÷ 30%** | 1 |
| 단일 LP 집중 한도 | 그 LP 예치 + amount ≤ **풀 예치 한도 × 25%** | 2 |

초과분은 전액 거절이 아니라 **한도까지 수용하고 나머지만 거절**합니다. 시점 9b에서 LP-04가 12,000 중 4,166만 받는 것이 이 경우입니다.

> 이 값들이 왜 이렇게 정해졌는지 → Notion 6번 「유동성·위험·수익 구조」

---

### 8. 데모 구조

#### 사전 실행 후 재생

**방문자의 클릭은 어떤 경우에도 새 트랜잭션을 만들지 않습니다.**

시나리오 전체를 테스트넷에서 **미리 한 번 실행**하고, 각 시점의 상태를 JSON으로 떠 둡니다. 제출된 웹사이트는 그 기록을 재생할 뿐입니다.

이렇게 하는 이유는 셋입니다.

1. 방문자마다 가스가 필요하지 않습니다
2. 여러 명이 동시에 접속해도 서로의 상태를 덮어쓰지 않습니다
3. 서버가 필요 없습니다. 심사 중 서버가 꺼질 걱정이 없습니다

**데모 실행 시점에 DB는 필요하지 않습니다.** DB는 데이터를 만드는 단계에서만 씁니다. 그래서 Supabase 무료 플랜이 일시 정지되어도 데모는 돕니다.

#### 13개 시점

| 시점 ID | 내용 |
|---|---|
| `t0` | 사용자 앱 환불 체험 |
| `t1` | 1년 정상 운영 |
| `t2` | 최종 손실 → 워터폴 |
| `t3` | 상관 손실 → 상한 발동 |
| `t3b` | 회수 정산 |
| `t4` | LP 출금 · 유동성 — 요청 직후, 대기분이 남아 있는 상태 |
| `t4b` | D+5 정산 — 만기 도래분 상환, 대기분 지급, 출금 완료 |
| `t5` | 지연 후 정상 상환 |
| `t6` | 외부 운용처 동결 |
| `t7` | 체인 성공 · DB 실패 |
| `t8` | 부정 요청 거절 |
| `t9` | 발급사가 늘어날 때 |
| `t9b` | 자본이 몰려올 때 |

**`t0`은 선택이 아니라 선행 조건입니다.** 사용자 앱의 예약 취소가 `REF-2026-001`을 실제로 선지급하고 상환하며, 시점 7과 8이 이 키를 참조합니다. 특히 시점 8의 「이미 사용된 refundKey 재제출」은 그 키가 실제로 사용된 뒤에만 성립합니다.

#### 스냅샷은 델타가 아니라 전체 상태

시점을 클릭하면 **이전 상태에 변화를 더하는 것이 아니라 상태를 통째로 교체**합니다. 그래서 순서를 건너뛰어도 결과가 같습니다. 4번을 먼저 눌러도 "1~3이 일어난 뒤의 4번 상태"가 정확히 나옵니다.

**12개 스냅샷은 한 회차의 연속 실행(`t0` → `t9b`)에서 전부 생성합니다.** 일부만 재생성하는 것은 금지입니다. 하나를 고치면 그 뒤의 모든 시점이 영향을 받기 때문입니다.

각 스냅샷에 `runId`를 기록하고, 프론트는 `runId`가 섞이면 로드를 거부합니다.

시나리오 순서를 바꾸면 각 시점이 나타내는 누적 상태가 달라지므로 **재번호가 아니라 전체 재실행**입니다.

#### 환불 체험 재생 규칙

- 사용자 앱의 환불 체험은 **횟수 제한 없이** 다시 볼 수 있습니다. 비교 스위치(기존 카드 ↔ 즉시 환불 적용)를 전제로 한 구조라 1회 제한은 두지 않습니다.
- 다시 보기는 재실행이 아니라 **첫 상태를 다시 불러오는 이동**입니다. 운영 화면도 함께 그 상태를 보여줍니다.
- 들어가기 직전 상태를 통째로 보관했다가 **돌아가기 한 번으로 수치와 확인 기록까지 그대로 복원**합니다.
- 다시 보기 중에는 시나리오 목록을 숨기고 돌아가기만 제공합니다.
- 재생해도 **같은 트랜잭션 해시**가 표시됩니다. 같은 `refundKey`는 한 번만 반영된다는 멱등성 설계가 화면에서 그대로 드러나는 지점입니다.

#### 3단계 자동 강등

```
1단계  사전 실행 기록 재생         (기본)
2단계  라이브 실행                 (선택 구현)
3단계  오프라인 Mock               (최후 보루)
```

1단계만으로 제출 요건을 충족합니다. 2·3단계는 여유가 있을 때 붙입니다.

> 상세 → Notion 9번 「데모 화면과 시나리오」, 5번 「데이터 흐름과 저장 구조」

---

### 9. 시스템 구성과 소유권

```
┌──────────────┐        ┌──────────────┐        ┌──────────────┐
│  원장 DB      │  ───▶  │   어댑터      │  ───▶  │   컨트랙트    │
│  (Supabase)  │        │  (TypeScript)│        │  (Solidity)  │
│   C(민재)     │        │   C(민재)     │        │   B(민서)     │
└──────────────┘        └──────────────┘        └──────┬───────┘
       ▲                                               │
       │              상환 확인 → 원장 반영              │
       └───────────────────────────────────────────────┘
                                                       │
                                            실행 스크립트가 시점마다
                                            스냅샷을 뜬다
                                                       │
                                                       ▼
                                            ┌──────────────────┐
                                            │  스냅샷 번들 JSON  │
                                            └────────┬─────────┘
                                                     │
                                            ┌────────▼─────────┐
                                            │   프론트엔드       │
                                            │   (React)        │
                                            │     A(서진)       │
                                            └──────────────────┘
```

#### 소유권

**내 영역 밖 파일은 고치지 않습니다.** 고쳐야 하면 소유자에게 말합니다.

| 경로 | 소유 | 내용 |
|---|---|---|
| `packages/contracts/` | **B(민서)** | Solidity, Foundry 테스트, 배포·실행 스크립트 |
| `packages/adapter/` | **C(민재)** | 변환 · EIP-712 서명 · 제출 · 상환 반영 |
| `packages/ledger/` | **C(민재)** | DB 스키마, 가상 데이터 생성, 조회 |
| `packages/web/` | **A(서진)** | 프론트엔드 |
| `shared/abi/` | **B(민서)** | ABI, EIP-712 타입 정의 |
| `shared/types/` | **A(서진)** | 스냅샷 · RUNBOOK 타입 |
| `shared/copy.ts` | **A(서진)** | 화면 문구 (en / ko) |
| `shared/snapshots/` | **A(서진)** (형식) / **B(민서)** (생성) | 샘플 번들과 실제 번들 |
| `shared/labels/` | **C(민재)** | 해시 → 라벨 매핑 |
| `scripts/` | **A(서진)** | 검증 스크립트 |
| `docs/` | **A(서진)** | 문서 |

---

### 10. 공유 계약과 착수 합의

**이 장이 이 문서에서 가장 중요합니다.**

#### 10.1 Day 0 — 착수 전에 정해야 할 것

모여서 정하지 않습니다. **「제안 → 기한 내 이의 없으면 확정」** 방식으로 갑니다.

| 항목 | 담당 | 기한 | 정하지 않으면 |
|---|---|---|---|
| 레포 폴더 구조와 소유권 | A(서진) | 9/19 | 같은 파일을 두 명이 고칩니다 |
| `.env` 키 이름 | A(서진) | 9/19 | 각자 다르게 부릅니다 |
| 스냅샷 JSON 스키마 + 샘플 번들 | A(서진) | 9/20 | 프론트를 두 번 만듭니다 |
| 식별자 규칙 (refundId, refundKey) | **B(민서)** | 9/20 | 세 사람이 다른 키를 만듭니다 |
| EIP-712 타입 정의 | **B(민서)** | 9/20 | 서명 검증이 전부 실패합니다 |
| 금액 단위와 변환 지점 | **B(민서)** | 9/20 | 100배 차이가 납니다 |
| 포지션 상태값 이름 | **B(민서)** | 9/20 | enum · DB 컬럼 · 화면 라벨이 어긋납니다 |
| 이벤트 · 함수 · 에러 (ABI 초안) | **B(민서)** | 9/21 | C(민재)의 파싱과 프론트 표시가 안 맞습니다 |
| DB 테이블 정의 | C(민재) | 9/21 | |

**절반 이상이 B(민서) 소유입니다. 그래서 B(민서)의 첫 작업은 컨트랙트 구현이 아니라 인터페이스 확정입니다.**

#### 10.2 공유 방식

```
1. 소유자가 초안을 만든다
2. shared/ 에 올리고 PR을 연다       제목: [shared] EIP-712 타입 정의 초안
3. 팀에 한 줄 알린다                 "올렸습니다. 내일 저녁까지 이견 없으면 확정합니다"
4. 기한 안에 이의 없으면 확정
5. develop에 머지하고 "확정" 알림
```

**기한을 반드시 붙이세요.** 비동기에서 가장 흔한 실패는 아무도 답을 안 해서 멈추는 것입니다. 기한이 있으면 침묵이 동의가 되고 일이 굴러갑니다.

**확정 후 변경**은 같은 절차를 다시 밟되, 그 계약에 의존하는 사람에게 **직접 알립니다.** PR만 올리고 끝내면 상대는 모르고 옛 버전으로 계속 만듭니다.

#### 10.3 합의 전에도 할 수 있는 것

완전히 막히지는 않습니다.

- **B(민서)** — Foundry 설치, 지갑 13개 파생, faucet 수급, 프로젝트 스켈레톤
- **C(민재)** — Supabase 프로젝트, 환경 세팅, 테이블 생성
- **A(서진)** — 프론트 스켈레톤, 화면 이식, `copy.ts` 영문 작성

#### 10.4 스냅샷 스키마와 샘플 번들

`shared/snapshots/sample/`에 **12개 JSON이 이미 들어 있습니다.** 값은 위 검증 수치 표 그대로입니다.

> **B(민서)와 C(민재)의 실행 스크립트는 이 폴더와 똑같은 형태의 결과를 만들어야 합니다.**
> 필드 이름 · 구조 · 타입이 하나라도 다르면 프론트가 읽지 못합니다.

작업 중 언제든 스스로 확인할 수 있습니다.

```bash
pnpm verify:bundle shared/snapshots/sample
pnpm verify:bundle shared/snapshots/run-20260925
```

검증 항목은 이렇습니다.

- 13개 시점이 모두 있는가
- `runId`가 전부 같은가
- 필수 필드가 다 있는가
- 각 시점의 LP 평가액 · 미상환 · 누적건수 · 손실률이 기준값과 일치하는가

**A(서진)에게 물어보지 말고 이 스크립트로 확인하세요.** 통과하면 맞는 겁니다.

#### 10.5 금액 단위

| 위치 | 단위 |
|---|---|
| 카드사 DB | 소수점 2자리 (`1000.00`) |
| 온체인 | 6자리 정수 (`1000000000`) |
| 스냅샷 JSON | USDC 단위 숫자 (`1000`) |
| 화면 표시 | USD, 소수점 0~2자리 |

**변환은 어댑터에서 한 번만** 합니다. 다른 곳에서 또 변환하면 100배가 틀어집니다.

#### 10.6 실제 값 예시 한 건

`REF-2026-001`이 각 단계에서 어떤 모습인지입니다. **이 표를 보고 어댑터를 만드시면 됩니다.**

```
① DB 행
   refund_id        "REF-2026-001"
   issuer_id        "HYBRID"
   acquirer_id      "ACQ-α"
   amount           1000.00
   confirmed_at     2026-09-14T10:22:00Z
   status           "confirmed"

② 어댑터 출력
   refundKey        keccak256("REF-2026-001")
   acquirerHash     keccak256("ACQ-α")
   amount           1000000000            ← 2자리 → 6자리
   maturity         <confirmed_at + 영업일 5일의 unix timestamp>
   nonce            <발급사별 증가값>
   deadline         <now + 10분>
   chainId          10143

③ 서명 페이로드 (EIP-712)
   { refundKey, issuer, acquirerHash, amount, maturity, nonce, deadline }

④ 온체인 이벤트
   AdvanceIssued(refundKey, issuer, amount, maturity)

⑤ 스냅샷 필드 — 체인에서 읽은 것이므로 해시뿐이다
   positions[0] = {
     refundKey: "0x9f41089dda…", issuer: "HYBRID", acquirerHash: "0xea2b5fc87a…",
     amount: 1000, fee: 3, issuerMargin: 200,
     poolCoverage: 800, state: "Advanced", txHash: "0x…"
   }

⑥ 라벨 매핑 — 읽는 값은 여기서만 나온다
   refunds["0x9f41089dda…"]  = { refundId: "REF-2026-001", amountUsdc: 1000 }
   acquirers["0xea2b5fc87a…"] = { acquirerId: "ACQ-α", displayName: "Acquirer α" }

⑦ 상환 확인 후 DB 반영
   creditLedger(refundKey, amount)   ← 멱등. 같은 키는 한 번만 반영
```

#### 10.7 포지션 상태값

**철자 그대로 씁니다.** 컨트랙트 enum, DB 컬럼, 스냅샷 필드, 화면 라벨이 전부 같아야 합니다.

```
Registered  →  Advanced  →  Repaid
                   ↓
                Overdue  →  Review  →  CoveredLoss
                                    →  CapHeld  →  RecoveryRecorded
```

발급사 상태는 `Active` → `MarginCall` → `Suspended` → `Deregistered`입니다. **담보가 0이 되면 `Suspended`입니다.**

#### 10.8 해시 → 라벨 매핑

체인에는 `refundId`와 `acquirerId`가 **keccak256 해시로만** 올라갑니다. 발급사 콘솔의 포지션 표에 필요한 사람이 읽는 값(건 번호 · 매입사 · 금액 · 날짜)은 체인에서 역산할 수 없습니다.

**그래서 스냅샷 번들에도 해시만 들어 있습니다.** 스냅샷은 체인에서 읽어 만드는 것이니 체인에 없는 값을 담을 수 없습니다.

> 공개 뷰는 반대입니다. 어느 발급사가 물렸는지 공개하지 않는 설계라 **일부러 해시를 그대로 보여줍니다.**

```json
{
  "runId": "run-20260925",
  "refunds":   { "0x9f41…": { "refundId": "REF-2026-001", "amountUsdc": 1000 } },
  "acquirers": { "0xea2b…": { "acquirerId": "ACQ-α", "displayName": "Acquirer α" } },
  "issuers":   { "HYBRID":  { "name": "HYBRID Travel Card", "region": "North America / Europe" } }
}
```

- 시점마다 뜨는 것이 아니라 전체 실행에 대해 **한 번 생성하는 정적 JSON**
- 번들에 나오는 **모든** `refundKey` · `acquirerHash` · 발급사 키가 여기 있어야 합니다. `pnpm verify:bundle`이 강제합니다
- 어댑터가 쓰는 해시 함수와 덤프가 쓰는 해시 함수는 **같은 모듈을 import**해야 합니다

**이름 규칙** — 셋이 같아야 하고, 검증 스크립트가 확인합니다.

```
runId  =  스냅샷 폴더 이름  =  라벨 파일 이름
```

만드는 법은 `shared/labels/README.md`에 코드로 적어뒀습니다. 샘플은 `shared/labels/sample.json`입니다.

---

## 2부 · 역할별 작업

### 11. A(서진) — 프론트엔드

#### 첫 30분

```bash
corepack enable pnpm          # pnpm이 없으면
cd packages/web
pnpm install
pnpm dev                      # http://localhost:5173 이 뜨면 성공
```

#### 산출물

| 파일 | 내용 |
|---|---|
| `packages/web/` | 데모 웹사이트 |
| `shared/types/snapshot.ts` | 스냅샷 타입 정의 |
| `shared/snapshots/sample/` | 샘플 번들 13개 (완료) |
| `shared/copy.ts` | 화면 문구 en / ko |
| `scripts/verify-bundle.ts` | 검증 스크립트 |
| `docs/` | 문서 |
| Envio 인덱서 | **선택.** 2단계 라이브 모드를 구현할 때만 |

#### 작업 순서

```
1. 스냅샷 스키마 확정 + 샘플 번들 공개        ← 9/20까지. B(민서)·C(민재)가 기다린다
2. 검증 스크립트 작성 + 공유
3. 프론트 골격 · 목업 CSS 이식
4. copy.ts 영문 작성                        ← 화면 만들면서 같이. 마지막에 몰지 말 것
5. 시점 이동 · 재생 로직
6. 환불 체험 다시 보기 · 돌아가기
7. 실제 번들로 교체
8. Vercel 배포
```

#### 완료 기준

- 샘플 번들만으로 13개 시점이 전부 렌더링된다
- 시점을 임의 순서로 눌러도 수치가 어긋나지 않는다
- 환불 체험 다시 보기 후 돌아가면 이전 상태가 완전히 복원된다
- 화면 문구가 전부 영문이다

#### 하지 말아야 할 것

- Tailwind로 다시 짜지 않습니다. 목업 CSS를 그대로 옮깁니다
- 차트 라이브러리를 쓰지 않습니다. 목업의 도넛 · 게이지 · 막대는 인라인 SVG입니다
- 스냅샷을 손으로 고치지 않습니다. 형식이 틀렸으면 스키마를 고치고 다시 생성 요청합니다

#### 구현 규칙

**열람 진행도(어느 시점을 확인했는지)는 스냅샷 바깥에 보관합니다.** 스냅샷은 사전 생성된 고정 파일이라 진행도를 담을 수 없고, 상태 교체로 사라지면 안 되는 값입니다.

#### 실제 번들로 교체할 때 — 작업 순서 7에서 같이 처리할 것

샘플 번들과 실제 번들은 **크기가 다릅니다.** 샘플은 시점 1의 1년치를 `YearlySummaryLoaded` 한 줄로 요약하고 있지만, 실제 실행에서는 그 자리에 **약 730건의 트랜잭션이 들어옵니다**(15번 참조). 교체 작업에는 다음 세 가지가 포함됩니다.

- **온체인 이벤트 표에 표시 상한을 넣습니다.** 지금은 전부 그립니다. 시점 1에서만 700줄이 되면 렌더가 느려지고, 심사위원은 스크롤만 하게 됩니다. 최근 N건 + 「더 보기」 형태로 자릅니다.
- **시나리오 1의 설계 포인트 문장을 고칩니다.** 「이 요약만 가정값이고 나머지 시나리오는 전부 실제 상태 변화입니다」는 실제 번들에서는 사실이 아닙니다 — 그때는 전부 실제 상태 변화입니다.

> 화면 구조 → Notion 9번 「데모 화면과 시나리오」

---

### 12. B(민서) — 스마트 컨트랙트

#### 첫 30분

```bash
curl -L https://foundry.paradigm.xyz | bash && foundryup
cd packages/contracts
forge build                   # 빌드가 통과하면 성공
```

그다음 바로 하세요.

```
1. 니모닉 1개 생성 → index 0~12에서 주소 13개 파생
2. 주소 목록을 팀에 공유 (개인키는 절대 공유 금지)
3. https://faucet.quicknode.com/monad 에서 9개 주소 각각 청구
4. QuickNode Build Plan 신청 → Monad Testnet 엔드포인트 → URL 팀 공유
```

**faucet은 오늘부터 매일 받으세요.** 12시간 쿨다운이라 하루에 모을 수 있는 양이 정해져 있습니다. 9/25에 800건을 돌리려면 그날 가스가 이미 있어야 합니다.

#### 산출물

| 파일 | 내용 |
|---|---|
| `packages/contracts/src/` | Solidity 컨트랙트 — 배포 2개(`Torna` · `MockUSDC`) + 라이브러리 |
| `packages/contracts/test/` | Foundry 테스트 |
| `packages/contracts/script/` | 배포 · 시나리오 실행 · 스냅샷 생성 |
| `shared/abi/` | **ABI와 EIP-712 타입 정의** |
| `shared/snapshots/run-*/` | 실제 스냅샷 번들 |

#### 작업 순서 · 1주차 — 만들기

| | 내용 | 규격이 있는 곳 | 언제 |
|---|---|---|---|
| **1** | **인터페이스 확정** — 식별자 규칙 · EIP-712 타입 · 금액 단위 · 상태값 이름 · 이벤트/함수/에러 코드<br>→ `shared/abi/`에 올리고 팀에 알린다 | `docs/interface-draft.md` | 9/20~21 |
| **2** | 컨트랙트 골격 — 발급사 등록 · 담보 예치 · 선지급 · 상환 | 10.7절 상태 전이 | 9/21~22 |
| **3** | 손실 처리 — `libraries/Waterfall.sol` 작성 | **7장 「손실 처리 규칙」** | 9/22 |
| **4** | 한도 검사 — `libraries/Limits.sol` 작성 | **7장 「한도 검사 규칙」** | 9/22 |
| **5** | Foundry 테스트 — 13개 시점의 수치를 전부 재현 | 7장 검증 수치표 | 9/22~23 |

**1번이 안 끝나면 C(민재)가 어댑터를 못 만듭니다.** 완벽하지 않아도 좋으니 9/21까지 초안을 올리세요. 이후 수정은 절차대로 알리면 됩니다.

인터페이스 초안이 `docs/interface-draft.md`에 있습니다. 백지에서 시작하지 마시고 이걸 다듬으세요. **확정 권한은 B(민서)에게 있습니다.**

3번과 4번은 계산식을 그대로 옮기면 되도록 7장에 적어뒀습니다. 근거가 궁금하면 Notion 6번을 보시되, **구현은 7장만 보고 하시면 됩니다.**

#### 컨트랙트 몇 개를 만드나 — 배포는 2개

```
packages/contracts/src/
├─ Torna.sol                 프로토콜 본체          ← 배포
├─ MockUSDC.sol              6자리 테스트 토큰      ← 배포
├─ TornaTypes.sol            struct · enum · error  (Torna에 포함)
└─ libraries/
   ├─ Waterfall.sol          손실 흡수 계산          (Torna에 포함)
   └─ Limits.sol             한도 계산               (Torna에 포함)
```

**`MockUSDC`가 필요한 이유** — Monad Testnet에는 우리가 쓸 USDC가 없습니다. 풀이 실제로 토큰을 주고받아야 선지급과 상환이 성립하므로, **소수점 6자리 ERC20을 하나 배포해서 씁니다.** 배포 직후 LP 지갑과 발급사 지갑에 초기 잔액을 민팅합니다.

**라이브러리로 분리하는 이유** — `Waterfall`과 `Limits`는 상태를 갖지 않는 **순수 계산 함수 묶음**입니다. 따로 배포되지 않고 `Torna.sol` 안으로 들어갑니다. 분리하는 이유는 **Foundry에서 계산만 따로 테스트할 수 있기 때문**입니다.

```solidity
// 컨트랙트를 배포하지 않고 워터폴 계산만 검증할 수 있다
function test_Waterfall_담보가_마진을_못내면_풀이_떠안는다() public {
    (uint256 margin, uint256 reserve, uint256 lp) =
        Waterfall.absorb(1000e6, 600e6, 0);      // coverage, 담보, 준비금
    assertEq(lp, 1200e6);
}
```

수치가 안 맞을 때 **컨트랙트 전체가 아니라 계산식만 들여다보면 됩니다.** 이게 디버깅 시간을 크게 줄입니다.

#### 어느 규칙이 어느 함수에 들어가나

| 7장 규칙 | 구현 위치 | 호출되는 곳 |
|---|---|---|
| 워터폴 | `Waterfall.absorb()` | `finalizeCoveredLoss()` |
| 단일 사건 상한 | `Waterfall.applyCap()` | `finalizeCoveredLoss()` |
| 회수 정산 | `Waterfall.settleRecovery()` | `recordRecovery()` |
| 발급사 유효 한도 | `Limits.effectiveLimit()` | `advance()` |
| 매입사 노출 한도 | `Limits.acquirerRoom()` | `advance()` |
| LP 예치 · 집중 한도 | `Limits.depositRoom()` | `depositLiquidity()` |

함수 이름은 예시입니다. **바꾸셔도 되지만 `shared/abi/`에 확정본을 올릴 때 같이 알려주세요.**

#### 권한은 컨트랙트를 나누지 않고 역할로 처리합니다

검증자를 별도 컨트랙트로 두지 않습니다. `AccessControl` 한 줄이면 됩니다.

```
DEFAULT_ADMIN_ROLE   배포자. 발급사 등록 · 파라미터
VERIFIER_ROLE        검증자. openReview · finalizeCoveredLoss · recordRecovery
SUBMITTER_ROLE       어댑터 제출자. advance · repay
```

지갑 index 0 · 1 · 2가 각각 여기에 붙습니다. **컨트랙트를 나누면 배포 주소가 늘고 호출이 복잡해질 뿐 얻는 게 없습니다.**

#### 작업 순서 · 2주차 — 돌리기

**배포 · 실행 · 스냅샷은 별개 작업이 아니라 하나의 스크립트입니다.**

컨트랙트가 가진 상태는 언제나 「현재」뿐입니다. `t9b`까지 다 돌린 뒤에 「`t3` 시점의 준비금은 얼마였나」를 물어볼 수 없습니다. 이미 덮여 있습니다. **그래서 실행과 스냅샷이 시점마다 번갈아 갑니다.**

```
① 배포                                    ← 한 번
     · MockUSDC 배포 → LP·발급사 지갑에 초기 잔액 민팅
     · Torna 배포 → 역할 부여 → 발급사 5곳 등록 → 담보 예치
      ↓
② 반복 ─────────────────────────────────────────────
     t0    트랜잭션 실행  →  상태 읽기  →  t0.json 저장
     t1    트랜잭션 실행  →  상태 읽기  →  t1.json 저장
     ...   t3b · t4b · t9b 는 앞 시점의 두 번째 단계다. 같은 시나리오를 이어
           실행하다가 중간에 한 번 더 뜬다 (예: t4 = 출금 요청 직후,
           t4b = D+5 정산 직후)
     ...
     t9b   트랜잭션 실행  →  상태 읽기  →  t9b.json 저장
   ─────────────────────────────────────────────────
      ↓
③ C(민재)가 해시 → 라벨 매핑을 덤프
      ↓
④ pnpm verify:bundle shared/snapshots/<runId>
      ↓
⑤ 통과하면 커밋하고 팀에 알린다
```

②의 「상태 읽기」는 `poolState()` · `issuerOf()` · `positionOf()` 를 호출하고 이벤트 로그를 읽어 스냅샷 스키마 모양으로 조립하는 것입니다. 특별한 도구는 없고 **직접 짜는 스크립트**입니다.

**스냅샷에는 체인에서 읽은 것만 담습니다.** 포지션에는 `refundKey` 와 `acquirerHash` 가 들어가고 `refundId` · 매입사 이름은 들어가지 않습니다 — 체인에 없기 때문입니다. 읽는 값은 C(민재)의 라벨 매핑에서 나옵니다. **DB를 참조해 채워 넣지 마세요.** 검증 스크립트가 잡아냅니다.

트랜잭션은 총 약 800건이고, 그중 730건쯤이 `t1`(1년치 365건의 선지급과 상환)입니다.

#### 두 번 돌립니다 — 로컬 리허설 → 테스트넷 본 실행

| | 어디서 | 가스 | 목적 |
|---|---|---|---|
| **리허설** | 로컬 `anvil` | 없음 | `verify:bundle`이 통과할 때까지 몇 번이고 반복 |
| **본 실행** | Monad Testnet | 실제 소모 | 제출물이 되는 기록 |

**테스트넷에서 처음 돌려보면 실패할 때마다 가스를 태웁니다.** 로컬에서 12시점이 전부 통과한 다음에 넘어가세요.

**중간에 실패하면 처음부터 다시입니다.** `t5`에서 터졌다고 `t5`만 다시 할 수 없습니다 — `t6` 이후가 그 결과 위에 쌓이기 때문입니다. 새 `runId`로 배포부터 다시 돌립니다.

9/24에 10건짜리 소규모 실행을 먼저 하는 이유가 이것입니다. 건당 가스 실소모량을 재고, 실패 지점을 하루 앞에서 발견하기 위해서입니다.

#### 완료 기준

- `forge test` 전부 통과
- anvil(로컬)에서 13개 시점을 순서대로 실행해 스냅샷 13개가 생성된다
- `pnpm verify:bundle <생성된 폴더>` 통과

#### 하지 말아야 할 것

- **서명 검증을 직접 구현하지 마세요.** OpenZeppelin의 `ECDSA`와 `EIP712`를 씁니다. 손으로 짜면 거의 확실히 틀립니다
- 어댑터 코드를 고치지 않습니다. 어댑터가 잘못 보내면 C(민재)에게 말합니다
- 스냅샷 스키마를 임의로 바꾸지 않습니다

#### 권장 스택

```
Foundry (forge, cast, anvil)
Solidity ^0.8.24
OpenZeppelin Contracts — AccessControl, ECDSA, EIP712
viem — 스냅샷 생성 스크립트
```

> 컨트랙트 설계 → Notion 4번 「시스템 및 스마트 컨트랙트 설계」

---

### 13. C(민재) — 어댑터와 원장 DB

#### 첫 30분

```bash
corepack enable pnpm
cd packages/ledger
pnpm install
```

그다음 바로 하세요.

```
1. supabase.com 에서 프로젝트 생성 (무료 플랜)
2. 연결 문자열을 .env 에 넣는다 (커밋 금지)
3. 팀에게 프로젝트 조회 권한 부여 — A(서진)과 B(민서)도 데이터를 볼 수 있어야 합니다
```

#### 산출물

| 파일 | 내용 |
|---|---|
| `packages/ledger/schema.sql` | DB 테이블 정의 |
| `packages/ledger/seed/` | 가상 데이터 생성 스크립트 |
| `packages/ledger/src/` | 조회 · 반영 함수 |
| `packages/adapter/src/` | 변환 · 서명 · 제출 |
| `shared/labels/` | 해시 → 라벨 매핑 |

#### DB 테이블

```
cardholders      사용자
transactions     결제 · 취소 이력
refunds          환불 건            ← 핵심
ledger_credits   잔액 반영 기록      ← UNIQUE(refund_key)
issuers          발급사             key · name · region
acquirers        매입사             acquirer_id · display_name
```

`issuers.region` 과 `acquirers.display_name` 은 체인에 올라가지 않습니다. **화면에 띄우려면 라벨 덤프에 들어가야 하므로 DB에 있어야 합니다.** 이 두 테이블이 없으면 공개 뷰의 매입사 막대와 포지션 표에 해시만 뜹니다.

**`ledger_credits`에 `UNIQUE(refund_key)`를 반드시 거세요.** 같은 환불이 두 번 들어와도 한 번만 반영된다는 걸 이 제약으로 증명합니다. 시점 7(체인 성공 · DB 실패)이 보여주려는 성질이 바로 이것입니다.

#### 어댑터가 하는 일

**나가는 길**

```
refundId, acquirerId  →  keccak256 해시
금액                   →  소수점 2자리를 6자리로
confirmedAt + 영업일 5일 →  unix 만기
nonce · deadline · chainId 부착
EIP-712 서명
컨트랙트에 제출
```

**돌아오는 길**

```
체인에서 상환 확인  →  creditLedger(refundKey, amount)  →  DB 반영
```

#### 작업 순서

| | 내용 | 선행 조건 | 언제 |
|---|---|---|---|
| **1** | Supabase 프로젝트 + 테이블 생성 | 없음 | 9/19~20 |
| **2** | 가상 환불 데이터 **365건 + 시나리오용** 생성 | 식별자 포맷 | 9/21 |
| **3** | 어댑터 변환 — 해시 · 소수점 · 만기 | B(민서)의 **식별자 규칙** | 9/22 |
| **4** | EIP-712 서명 | B(민서)의 **타입 정의** | 9/22 |
| **5** | 제출 · 상환 확인 · 원장 반영 (`creditLedger`) | ABI | 9/22~23 |
| **6** | 해시 → 라벨 매핑 덤프 | 위 전부 | 9/23 |

#### 6번 — 라벨 매핑 덤프

체인에는 해시만 올라가므로 **스냅샷 번들에도 해시만 들어 있습니다.** 발급사 콘솔과 검증자 화면에 읽는 값을 띄우려면 이 파일이 있어야 하고, 없으면 포지션 표에 `0x9f41…0e1f`만 뜹니다.

실행이 끝난 뒤 **SELECT 한 번, 파일 쓰기 한 번**입니다.

```ts
// packages/ledger/scripts/dump-labels.ts
import { keccak256 } from '@torna/adapter/hash';   // ← 어댑터와 같은 함수를 import

const runId = process.env.RUN_ID!;
const out = { runId, refunds: {}, acquirers: {}, issuers: {} };

const { data: refunds } = await supabase
  .from('refunds').select('refund_id, amount, confirmed_at');

for (const r of refunds) {
  out.refunds[keccak256(r.refund_id)] = {
    refundId: r.refund_id, amountUsdc: Number(r.amount), confirmedAt: r.confirmed_at,
  };
}
// acquirers · issuers 도 같은 방식

fs.writeFileSync(`shared/labels/${runId}.json`, JSON.stringify(out, null, 2));
```

**해시 함수를 다시 짜지 마세요.** 어댑터가 쓰는 모듈을 그대로 `import`합니다. 다르면 짝이 전부 어긋나고, 화면에는 해시만 뜹니다.

**이름을 맞추세요.** `runId` = 스냅샷 폴더 이름 = 라벨 파일 이름. 검증 스크립트가 확인합니다.

자세한 형식과 실수 목록은 `shared/labels/README.md`에 있습니다.

#### 완료 기준

- 365건 + 시나리오용 데이터가 DB에 있다
- 어댑터가 환불 1건을 변환해 서명하고, B(민서)의 로컬 컨트랙트가 그 서명을 검증에 통과시킨다
- 같은 `refundKey`를 두 번 반영해도 잔액이 한 번만 증가한다
- 라벨 덤프를 돌린 뒤 `pnpm verify:bundle`이 **「라벨 매핑 — 전부 덮임」**을 출력한다

#### 하지 말아야 할 것

- **EIP-712 구조체를 직접 작성하지 마세요.** B(민서)가 `shared/abi/`에 내보낸 타입을 `import`해서 씁니다. 양쪽에서 각자 손으로 적으면 한 글자씩 어긋나고, 서명 검증이 전부 실패합니다
- 컨트랙트 코드를 고치지 않습니다
- 금액 변환을 어댑터 밖에서 또 하지 않습니다

#### 권장 스택

```
Node 20+ · TypeScript
viem — 서명과 제출
Supabase (Postgres) — @supabase/supabase-js 또는 postgres.js
tsx — 스크립트 실행
zod — 입력 검증 (선택이지만 권장)
```

**이건 권장입니다.** 더 익숙하거나 더 빠른 대안이 있으면 바꾸셔도 됩니다. 다만 `shared/`의 형식과 산출물 형태는 바뀌지 않아야 하고, 바꾸기 전에 팀에 알려주세요.

> 데이터 흐름 → Notion 5번 「데이터 흐름과 저장 구조」

---

## 3부 · 실행

### 14. 실행 환경

#### 네트워크

```
Monad Testnet
Chain ID    10143
통화         MON
익스플로러    https://testnet.monadvision.com
             https://testnet.monadscan.com
Faucet      https://faucet.monad.xyz
            https://faucet.quicknode.com/monad
            https://www.alchemy.com/faucets/monad-testnet
```

#### RPC

| 엔드포인트 | 한도 |
|---|---|
| `https://testnet-rpc.monad.xyz` | 50 rps (`eth_call` · `estimateGas`는 25) |
| `https://rpc.ankr.com/monad_testnet` | 300 / 10초 |
| QuickNode Build Plan | 해커톤 혜택. **800건 일괄 실행에 필요합니다** |

일상적인 개발과 단건 테스트는 공용 RPC로 충분합니다. QuickNode는 대량 실행용으로 아껴 쓰세요.

#### 지갑

**니모닉 1개에서 index 0~12까지 13개**를 파생합니다.

| index | 역할 | 가스 |
|---|---|---|
| 0 | 배포자 | 필요 |
| 1 | 검증자 | 필요 |
| 2 | 어댑터 제출자 | 필요 |
| 3~7 | 발급사 5곳 | **불필요** (EIP-712 서명만) |
| 8~12 | LP 6곳 | 필요 |

가스가 필요한 건 **9개**입니다.

#### 가스 예산

- 배포 3~5건 · 시나리오 약 800건 → **총 15 MON 이상** 목표
- Monad는 실제 사용량이 아니라 **gas limit 기준으로 과금**합니다. 넉넉히 잡으면 그만큼 그대로 나갑니다. 스크립트에서 실측값에 맞춰 조이세요
- 9/24 소규모 실행(10건)으로 건당 실소모량을 측정한 뒤 예산을 확정합니다

#### 테스트넷 리셋 리스크

Monad 테스트넷은 과거 genesis부터 리셋된 적이 있습니다. 리셋되면 배포된 컨트랙트와 잔액이 사라집니다.

**대응**: 스냅샷 번들을 레포에 커밋해 두면 화면은 계속 돕니다. 재실행은 여유가 있을 때 합니다. 그러니 모아두기만 하고 실행을 미루는 것보다 **확보되는 대로 빨리 돌리는 편이 안전합니다.**

---

### 15. 시나리오 실행 절차

**최종 실행은 한 사람이 한 회차로 돌립니다.** `runId`가 섞이면 안 되기 때문입니다. 가스를 가진 **B(민서)가 C(민재)의 어댑터를 라이브러리로 가져다 쓰는 형태**로 실행합니다.

C(민재)가 자기 어댑터를 따로 돌려 만든 데이터는 개발 중 검증용이고, **최종 번들에는 들어가지 않습니다.**

#### 절차

```
1. runId를 정한다                       예: run-20260925
2. DB를 초기 상태로 되돌린다
3. 컨트랙트를 배포한다
4. t0부터 t9b까지 순서대로 실행한다
5. 각 시점에서 스냅샷을 뜬다             → shared/snapshots/run-20260925/
6. 해시 → 라벨 매핑을 덤프한다           → shared/labels/run-20260925.json
7. pnpm verify:bundle shared/snapshots/run-20260925
8. 통과하면 커밋한다
```

#### 규칙

- **부분 재생성 금지.** 시점 5를 고치면 6~9b를 전부 다시 떠야 합니다
- 모든 스냅샷의 `runId`가 같아야 합니다
- 검증 스크립트를 통과하지 못한 번들은 커밋하지 않습니다
- 트랜잭션은 약 800건입니다. 그중 약 730건이 시점 1(1년치 365건의 선지급과 상환)입니다

---

### 16. 타임라인과 선후 관계

#### 1주차 · 각자 파트 — 9/23(수)까지

| 날짜 | A(서진) | B(민서) | C(민재) |
|---|---|---|---|
| **9/19 토** | 레포 · 폴더 구조 공유, `.env` 키 확정 | 환경 세팅, 지갑 13개 파생, **faucet 시작** | Supabase 프로젝트 생성, 환경 세팅 |
| **9/20 일** | 샘플 번들 + 검증 스크립트 | **식별자 · EIP-712 · 상태값 · 금액단위 확정 후 공유** | DB 테이블 정의 |
| **9/21 월** | 프론트 골격, 화면 이식 | **ABI 초안 확정 후 공유**, 컨트랙트 골격 | 가상 데이터 365건 생성 |
| **9/22 화** | 시점 이동 · 재생 로직 | 컨트랙트 + forge 테스트 | 어댑터 변환 · 서명 |
| **9/23 수** | 샘플 번들로 전 화면 동작 | anvil에서 12시점 통과 | 어댑터 단위 검증 통과 |

#### 2주차 · 통합 — 9/27(일)까지

| 날짜 | 내용 |
|---|---|
| **9/24 목** | 어댑터 ↔ 컨트랙트 연결, 테스트넷 소규모 실행(10건)으로 가스 실측 |
| **9/25 금** | 전체 실행 약 800건, 번들 생성, `verify:bundle` 통과 |
| **9/26 토** | 실제 번들로 교체, 전 시나리오 클릭 검수, Vercel 배포 |
| **9/27 일** | 버그 수정 · 영문 카피 최종 · README 완성 · **예비일** |

#### 9/28(월) 이후 — 피치덱 · 3분 영상

#### 9/22에 미니 통합을 하세요

**각자 파트가 완성되지 않아도 좋습니다.** 환불 3건짜리로 C(민재)의 DB → 어댑터 → B(민서)의 컨트랙트 → 스냅샷 → A(서진)의 프론트까지 **끝에서 끝까지 한 번 관통**시킵니다.

통합 문제는 거의 전부 인터페이스에서 나옵니다. 서명 필드 순서, 소수점 자릿수, 상태값 철자 같은 것들이요. 9/24에 처음 붙여보고 발견하면 되돌릴 시간이 없습니다. **9/22에 3건으로 발견하면 이틀이 남습니다.**

#### 위험 구간

| 위험 | 영향 | 대응 |
|---|---|---|
| B(민서)의 인터페이스가 9/21까지 안 나옴 | 전체가 밀립니다 | 9/20 밤 기준 점검. 늦어지면 A(서진)이 임시 초안으로 확정 |
| 9/25 전체 실행 실패 | 예비일이 9/27 하루뿐 | 9/24 소규모 실행으로 하루 앞에서 발견 |
| 테스트넷 리셋 | 반나절~하루 재실행 | 번들을 커밋해 두면 화면은 계속 돕니다 |
| RPC 한도 | 800건 일괄 실행이 막힘 | QuickNode 엔드포인트를 9/24 전에 확보 |

**예비일이 사실상 9/27 하루입니다.** 위 넷 중 둘 이상이 겹치면 피치덱 준비 시간을 먹습니다.

#### 영문 작업을 마지막에 몰지 마세요

`copy.ts` 영문은 9/21 프론트 골격 작업과 같이 씁니다. 9/27에 200개 문자열을 번역하려 들면 그날 하루가 날아갑니다. README도 9/24~26 사이에 조금씩 채우세요.

---

### 17. 자주 꼬이는 지점

겪을 게 뻔한 것들입니다. **증상이 보이면 첫 확인 사항부터 보세요.**

| 증상 | 첫 확인 |
|---|---|
| 서명 검증이 항상 실패한다 | EIP-712 타입의 **필드 이름과 순서**가 컨트랙트와 글자 단위로 같은가. C(민재)가 직접 작성하지 않고 `shared/abi/`에서 import했는가 |
| 금액이 100배 또는 1/100이다 | 소수점 변환을 **어댑터 밖에서 또** 하고 있지 않은가 |
| 금액이 1,000,000배 틀리다 | 2자리 → 6자리 변환 계수가 `10^4`인데 `10^6`을 썼거나 그 반대 |
| 프론트가 스냅샷을 못 읽는다 | `pnpm verify:bundle`을 돌렸는가. 필드 이름이 샘플과 같은가 |
| 시점을 건너뛰면 수치가 이상하다 | 스냅샷을 **델타로** 적용하고 있지 않은가. 전체 교체여야 한다 |
| 시점마다 `runId`가 다르다 | 한 회차로 돌리지 않고 부분 재실행했다. 전부 다시 |
| 포지션 표에 해시만 보인다 | 해시 → 라벨 매핑 파일을 로드했는가 |
| 트랜잭션이 중간에 멈춘다 | RPC 한도(공용 50 rps). QuickNode 엔드포인트를 쓰는가 |
| 가스가 모자란다 | gas limit을 실측값에 맞춰 조였는가. Monad는 limit 기준 과금 |
| 배포한 컨트랙트가 사라졌다 | 테스트넷 리셋. 커밋된 번들로 화면은 계속 돈다 |
| `.env`가 커밋됐다 | 즉시 팀에 알리고 키를 재발급. 되돌리기만으로는 이력에 남는다 |
| 상태값이 화면에 안 뜬다 | enum 철자가 10.7절과 같은가 |

**30분 이상 막히면 바로 팀에 올리세요.** 답이 없으면 **가정하고 진행하되 무엇을 가정했는지 적어두세요.** 멈춰 있는 게 가장 큰 손해입니다.

---

### 18. 제출물 체크리스트

제출 직전에 하나씩 지우세요.

```
[ ] 데모 사이트가 배포되어 있고 링크가 열린다
[ ] 13개 시점이 전부 정상 동작한다
[ ] 시점을 임의 순서로 눌러도 수치가 어긋나지 않는다
[ ] 환불 체험 다시 보기 → 돌아가기가 동작한다
[ ] 화면에 한글이 하나도 남아 있지 않다
[ ] README.md가 영문이고 실행 방법이 적혀 있다
[ ] 컨트랙트 주소와 익스플로러 링크가 README에 있다
[ ] .env가 커밋되어 있지 않다
[ ] main에 v1.0-submission 태그가 붙어 있다
[ ] 스냅샷 번들과 라벨 매핑이 커밋되어 있다
[ ] pnpm verify:bundle이 통과한다
[ ] 3분 기술 데모 영상 (영문)
[ ] 2분 피치 (영문)
[ ] 해커톤 제출 폼 작성 완료
```

---

## 부록 · Notion 문서 색인

설계 근거는 전부 Notion에 있습니다.

| 번호 | 제목 | 언제 보는가 |
|---|---|---|
| 1 | 문제 정의와 시장 검증 | 왜 이 문제를 푸는가 |
| 2 | 왜 블록체인인가 | 왜 온체인이어야 하는가 |
| 3 | 사용자 경험과 원장 구조 | 3단 원장, 사용자 화면 |
| 4 | 시스템 및 스마트 컨트랙트 설계 | 컨트랙트 구조, 상태 전이 |
| 5 | 데이터 흐름과 저장 구조 | 스냅샷 규칙, 라벨 매핑 |
| 6 | 유동성·위험·수익 구조 | 담보율 · 워터폴 · 한도의 근거 |
| 7 | 유휴자금 운용 설계 | 외부 운용, 서킷 브레이커 |
| 8 | MVP 범위와 실행 계획 | 범위 경계 |
| 9 | 데모 화면과 시나리오 | 시나리오 상세, 시점별 수치 |
| 10 | 결정 기록과 미해결 과제 | 왜 그렇게 정했는지 |

---

**문서 변경 이력**

| 판 | 날짜 | 내용 |
|---|---|---|
| 1.0 | 2026-09-19 | 최초 작성 |
