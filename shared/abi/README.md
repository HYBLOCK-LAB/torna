# `shared/abi/` — 소유: B(민서)

여기에 들어갈 것:

```
Torna.json        컴파일된 ABI
eip712.ts         EIP-712 타입 정의 — C(민재)가 import
events.ts         이벤트 이름과 파라미터 타입 — A(서진)이 import
reasons.ts        거절 사유 코드 → 이름
```

**기한 9/21.** 이게 없으면 C(민재)는 어댑터 서명부를, A(서진)은 이벤트 표시를 만들 수 없습니다.

초안이 `docs/interface-draft.md` 에 있습니다. 백지에서 시작하지 마시고 그걸 다듬으세요.

## C(민재)에게

**EIP-712 구조체를 직접 작성하지 마세요.** 여기 있는 `eip712.ts` 를 import 해서 씁니다.
양쪽에서 각자 손으로 적으면 한 글자씩 어긋나고, 서명 검증이 전부 실패합니다.
